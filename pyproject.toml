[project]
name = "radar-problemas"
version = "0.1.0"
description = "Priorização de candidatos a problema por score de frequência, intensidade, acesso e disposição a pagar."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
